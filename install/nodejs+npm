#sudo apt install nodejs
sudo apt install npm

#maybe better just install npm and then, latest nodejs with n module
sudo npm cache clean -f
sudo npm install -g n
sudo n stable

#check if installed correctly
nodejs -v
npm -v
